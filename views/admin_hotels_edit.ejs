<!doctype html>
<html lang="en">

<head>
    <!-- ... -->
    <!-- Include Bootstrap CSS and other required CSS files -->
    <!-- ... -->
</head>

<body>
    <h2>Edit Hotel</h2>
    <form action="/edit/hotel/<%= hotel.id %>" method="POST">
        <div>
            <label for="tour_name">Hotel Name:</label>
            <input type="text" id="tour_name" name="hotel_name" value="<%= hotel.name %>">
        </div>
         <div>
            <label for="tour_name">Hotel Price:</label>
            <input type="text" id="tour_name" name="hotel_price" value="<%= hotel.price %>">
        </div>
        <div>
            <label for="tour_description">Description:</label>
            <textarea id="tour_description" name="hotel_description" rows="4"
                cols="50"><%= hotel.description %></textarea>
        </div>
        <div>
            <label for="tour_image">Image URL:</label>
            <input type="text" id="tour_image" name="hotel_image" value="<%= hotel.image1 %>">
        </div>
        <!-- Add more form fields if needed, based on your schema -->
        <input type="submit" value="Update Hotel">
    </form>
</body>

</html>